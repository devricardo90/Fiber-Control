# FC-022 - DONE

## Objetivo
Reexecutar a validacao local/manual do MVP do Fiber Control apos a `FC-026`, para decidir com evidencia real se o baseline local ficou pronto para avancar para staging.

## Escopo encerrado
- repetir os gates tecnicos da API e da web sobre o baseline corrigido
- confirmar que a suite da API roda no banco oficial de testes `127.0.0.1:5442`
- confirmar que o banco de desenvolvimento `127.0.0.1:5440` permanece intacto apos `pnpm.cmd test`
- validar novamente o login seed `acesso@fibercontrol.local / Fiber@123456`
- validar `GET /health`, `GET /auth/me`, preflight CORS e as superficies minimas do MVP
- decidir formalmente se `FC-023` pode virar `READY`

## Fora de escopo mantido
- deploy/staging
- configuracao de provider
- novas features de produto
- expansao do frontend para modulos fora do MVP

## Comandos executados
- `node -v`
- `pnpm.cmd prisma:generate` em `apps/api`
- `pnpm.cmd lint` em `apps/api`
- `pnpm.cmd build` em `apps/api`
- `pnpm.cmd prisma:migrate:deploy` em `apps/api` com `.env.test`
- `pnpm.cmd test` em `apps/api` com `.env.test`
- consulta direta ao banco de desenvolvimento em `127.0.0.1:5440`
- `pnpm.cmd lint` em `apps/web`
- `pnpm.cmd build` em `apps/web`
- boot local da API com `node dist/server.js`
- boot local da web com `node node_modules/next/dist/bin/next start --hostname web.fiber-control.localhost --port 3000`
- smoke manual de auth, CORS, rotas web e endpoints minimos do MVP

## Resultado das validacoes
- `pnpm.cmd prisma:generate` em `apps/api`: PASS com warning de engine Node `24.x` vs ambiente `v22.21.1`
- `pnpm.cmd lint` em `apps/api`: PASS
- `pnpm.cmd build` em `apps/api`: PASS
- `pnpm.cmd prisma:migrate:deploy` em `127.0.0.1:5442`: PASS
- `pnpm.cmd test` em `apps/api`: PASS com `13` arquivos e `71` testes verdes
- consulta direta ao banco dev apos a suite:
  - `users = 4`
  - `customers = 4`
  - `payments = 5`
  - `regions = 3`
  - `acesso@fibercontrol.local` presente
- `pnpm.cmd lint` em `apps/web`: PASS
- `pnpm.cmd build` em `apps/web`: PASS
- `GET /health`: PASS
- `POST /auth/login` com seed local: PASS
- `GET /auth/me`: PASS
- preflight CORS para `http://web.fiber-control.localhost:3000`: PASS
- rotas web `/login`, `/dashboard`, `/customers`, `/customers/new`, `/payments`, `/payments/new`, `/alerts`, `/finance`, `/reports`, `/regions` e `/routes`: PASS `200`
- endpoints do MVP (`customers`, `payments`, `alerts`, `finance`, `reports`, `regions`): PASS

## Evidencias
- `docs/quality/fc-022-local-validation.md`
- `apps/api/vitest.config.ts`
- `apps/api/src/tests/payments.spec.ts`
- `apps/api/src/tests/fiscal-reminders.spec.ts`
- `backlog.md`
- `STATUS.md`
- `docs/ops/execution-log.md`
- `docs/ops/decisions.md`

## Limitacoes e neutralizacoes
- `apps/web` continua sem suite automatizada dedicada
- `apps/api` segue declarando Node `24.x`, embora os gates tenham passado em `v22.21.1`
- `Routes` continua sem backend dedicado; a validacao do MVP permanece limitada a overview web e contratos correlatos existentes
- modulos fora do MVP permanecem explicitamente fora de escopo

## Decisao final
- `FC-022` encerrada como `DONE`
- `FC-023` pode ser promovida para `READY`
- nenhum deploy foi iniciado nesta task

## Data e contexto do encerramento
- encerrada em 2026-04-24, apos reexecucao formal sobre o baseline corrigido pela `FC-026`
