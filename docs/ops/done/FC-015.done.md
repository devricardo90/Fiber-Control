# FC-015 - DONE

## Titulo
Auditar e consolidar a superficie Dashboard existente

## Data de conclusao
2026-04-06

## Responsavel
Agente Orquestrador Senior

## Classificacao formal
- `DONE`
- o escopo foi mantido apenas na overview de `Dashboard`
- nao houve redesign completo nem abertura de route preview

## Objetivo e escopo
- auditar a superficie `Dashboard` ja existente
- consolidar `dashboard-screen.tsx`
- preservar o reaproveitamento util ja existente
- aplicar apenas ajustes minimos de primitives, densidade, hierarquia e consistencia visual
- reabrir `/dashboard`
- manter route preview e analytics avancado fora de escopo

## Fora de escopo
- redesign completo de `Dashboard`
- refactor amplo de frontend
- abertura de route preview operacional
- abertura de smart analytics dedicado
- criacao de contrato backend novo de dashboard
- reabertura de `Routes`

## O que foi consolidado
- a overview de dashboard deixou de redirecionar para `/workspace` e passou a agregar os contratos reais de `finance`, `alerts`, `reports` e `regions`
- a tela foi consolidada com leitura operacional de receita, alertas, tendencia anual e concentracao regional
- a navegacao lateral passou a expor `Dashboard` como setima superficie de negocio reaberta
- a rota `/dashboard` deixou de estar neutralizada
- route preview e analytics avancado permaneceram explicitamente fora de escopo

## Evidencias objetivas
- [`dashboard/page.tsx`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/app/(app)/dashboard/page.tsx) agora renderiza `DashboardScreen`
- [`dashboard-screen.tsx`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/features/dashboard/components/dashboard-screen.tsx) foi consolidada para a linguagem operacional ativa com leitura real da API
- [`navigation.ts`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/config/navigation.ts) passou a expor `Dashboard` na sidebar
- [`route-guard.tsx`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/components/layout/route-guard.tsx) passou a permitir `/dashboard`

## Comandos executados
```text
pnpm.cmd lint
pnpm.cmd build
pnpm.cmd build
```

## Resultado de cada validacao
- auditoria da superficie Dashboard existente antes de editar: PASS
- `pnpm.cmd lint` em `apps/web`: PASS
- `pnpm.cmd build` em `apps/web`: FAIL no sandbox por `spawn EPERM`
- `pnpm.cmd build` em `apps/web` fora do sandbox: PASS
- testes de `apps/web`: N/A, `package.json` nao expoe script de teste nesta fotografia

## Observacoes operacionais
- a task preservou o reaproveitamento util do frontend e substituiu apenas a neutralizacao por uma overview agregada dos contratos reais do backend
- o build precisou rodar fora do sandbox por limitacao ambiental, nao por falha estrutural do frontend
- route preview e analytics avancado ficaram fora de escopo por governanca

## Criterios de aceite
- auditoria da superficie Dashboard existente executada antes de editar: PASS
- `dashboard-screen.tsx` consolidada na linguagem operacional aprovada: PASS
- `/dashboard` reaberta: PASS
- route preview e analytics avancado mantidos fora de escopo: PASS
- backlog, `STATUS.md` e `docs/ops/*` atualizados: PASS

## Impacto em negocio e arquitetura
- a setima superficie de negocio do frontend volta a ficar ativa sem quebrar a fundacao visual da FC-007
- a consolidacao continua orientada por reaproveitamento controlado e escopo pequeno

## PRs ou commits relevantes
- sem commit ou PR nesta execucao

## Proxima task oficial
- `FC-016` - Auditar e consolidar a superficie `Routes` existente
