# FC-023 - DONE

## Titulo
Staging Deployment Baseline

## Data de conclusao
2026-04-24

## Responsavel
Agente Orquestrador Senior

## Status final
- `DONE`

## Classificacao
- Deploy / Infrastructure
- prioridade `P0`

## Objetivo e escopo
- fechar a baseline minima de staging para web, API e banco
- consolidar runtime, env vars e regras operacionais de staging sem registrar secrets reais
- validar o `Manual External Operations Gate` do Neon
- decidir formalmente se a task poderia sair de `DOING` para `DONE`

## Fora de escopo
- deploy funcional em provedor pelo agente
- criacao automatizada de recursos externos
- dominio customizado
- hardening enterprise
- alteracao de codigo de produto

## O que foi consolidado
- `docs/ops/fc-023-staging-baseline.md` passou a registrar a baseline oficial de staging
- a divergencia de Node foi resolvida com Node `v24.15.0`
- Prisma `7` ficou consolidado com `DIRECT_URL` via `apps/api/prisma.config.ts` para CLI e migrations
- o runtime permaneceu definido sobre `DATABASE_URL` pooled do Neon
- o `Manual External Operations Gate` do Neon foi validado manualmente
- nenhum secret real foi registrado em documentacao ou arquivos versionados

## Comandos validados
- `node -v`
- `npm -v`
- `pnpm -v`
- `pnpm.cmd install`
- `pnpm.cmd prisma generate`
- `pnpm.cmd prisma migrate deploy`
- `pnpm.cmd build`
- `docker compose up -d`
- `pnpm.cmd test`

## Resultado das validacoes
- `node -v`: PASS com `v24.15.0`
- `npm -v`: PASS com `11.12.1`
- `pnpm -v`: PASS com `10.33.0`
- `pnpm.cmd install`: PASS
- `pnpm.cmd prisma generate`: PASS
- `pnpm.cmd prisma migrate deploy`: PASS
- `pnpm.cmd build`: PASS
- `docker compose up -d`: PASS
- `pnpm.cmd test`: PASS
- `Manual External Operations Gate` do Neon: `VALIDATED`

## Evidencias objetivas
- [`docs/ops/fc-023-staging-baseline.md`](/C:/Users/ricardodev/Desktop/app-fiber-control/docs/ops/fc-023-staging-baseline.md)
- [`docs/ops/decisions.md`](/C:/Users/ricardodev/Desktop/app-fiber-control/docs/ops/decisions.md)
- [`docs/ops/execution-log.md`](/C:/Users/ricardodev/Desktop/app-fiber-control/docs/ops/execution-log.md)
- [`docs/ops/session-handoff.md`](/C:/Users/ricardodev/Desktop/app-fiber-control/docs/ops/session-handoff.md)
- [`STATUS.md`](/C:/Users/ricardodev/Desktop/app-fiber-control/STATUS.md)
- [`backlog.md`](/C:/Users/ricardodev/Desktop/app-fiber-control/backlog.md)

## Limitacoes ou neutralizacoes
- a ausencia de manifests especificos de Render/Vercel no repositorio continua registrada como lacuna operacional conhecida
- a validacao do Neon dependeu de operacao humana fora do repositorio por desenho; isso foi tratado como gate manual controlado, nao como falha
- o encerramento desta task nao significa que staging ja foi publicado; significa que a baseline e os gates obrigatorios ficaram documentados e validados

## Contexto de encerramento
- `FC-023A` fechou o contrato documental de staging
- `FC-023B` fechou o contrato operacional/documental de Prisma, env vars e gate externo
- o humano confirmou o ambiente corrigido e os comandos finais em `PASS`
- com backlog, status, log operacional, handoff e baseline convergentes, a task passou a atender a definicao de `DONE`

## Impacto em negocio e arquitetura
- a baseline publica do projeto deixa de depender de inferencias sobre runtime e banco de staging
- a separacao entre `DATABASE_URL` pooled para runtime e `DIRECT_URL` direct para Prisma CLI fica formalizada
- o proximo ciclo pode focar em documentacao publica e portfolio sem reabrir a discussao tecnica de staging

## PRs ou commits relevantes
- sem commit ou PR nesta execucao

## Proxima task oficial
- `FC-024` volta a ser a `READY` oficial
