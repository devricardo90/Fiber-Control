# FC-016 - DONE

## Titulo
Auditar e consolidar a superficie Routes existente

## Data de conclusao
2026-04-06

## Responsavel
Agente Orquestrador Senior

## Classificacao formal
- `DONE`
- o escopo foi mantido apenas na overview de `Routes`
- nao houve redesign completo nem abertura de route planning

## Objetivo e escopo
- auditar a superficie `Routes` ja existente
- consolidar `routes-screen.tsx`
- preservar o reaproveitamento util do modulo sem manter linguagem visual fora da governanca
- aplicar apenas ajustes minimos de primitives, densidade, hierarquia e consistencia visual
- reabrir `/routes`
- manter route planning, live maps e operacao de campo fora de escopo

## Fora de escopo
- redesign completo de `Routes`
- refactor amplo de frontend
- abertura de route planning
- abertura de live tracking
- abertura de mapa interativo
- abertura de operacao de campo
- criacao de contrato backend novo para routes

## O que foi consolidado
- a overview de routes deixou de redirecionar para `/workspace` e passou a expor fila operacional de despacho
- a tela foi consolidada com resumo compacto, tabela de visitas e leitura objetiva de bloqueios
- a navegacao lateral passou a expor `Routes` como oitava superficie de negocio reaberta
- a rota `/routes` deixou de estar neutralizada
- route planning, live maps e operacao de campo permaneceram explicitamente fora de escopo

## Evidencias objetivas
- [`routes/page.tsx`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/app/(app)/routes/page.tsx) agora renderiza `RoutesScreen`
- [`routes-screen.tsx`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/features/routes/components/routes-screen.tsx) foi consolidada para a linguagem operacional ativa sem mock de mapa ou planning inflado
- [`routes-data.ts`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/features/routes/data/routes-data.ts) passou a sustentar a overview operacional minima da task
- [`navigation.ts`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/config/navigation.ts) passou a expor `Routes` na sidebar
- [`route-guard.tsx`](/C:/Users/ricardodev/Desktop/app-fiber-control/apps/web/src/components/layout/route-guard.tsx) passou a permitir `/routes`

## Comandos executados
```text
pnpm.cmd lint
pnpm.cmd build
pnpm.cmd build
```

## Resultado de cada validacao
- auditoria da superficie Routes existente antes de editar: PASS
- `pnpm.cmd lint` em `apps/web`: PASS
- `pnpm.cmd build` em `apps/web`: FAIL no sandbox por `spawn EPERM`
- `pnpm.cmd build` em `apps/web` fora do sandbox: PASS
- testes de `apps/web`: N/A, `package.json` nao expoe script de teste nesta fotografia

## Observacoes operacionais
- a task removeu a deriva visual da superficie e evitou simular capacidades sem backend real
- o build precisou rodar fora do sandbox por limitacao ambiental, nao por falha estrutural do frontend
- route planning, live maps e operacao de campo ficaram fora de escopo por governanca

## Criterios de aceite
- auditoria da superficie Routes existente executada antes de editar: PASS
- `routes-screen.tsx` consolidada na linguagem operacional aprovada: PASS
- `/routes` reaberta: PASS
- route planning, live maps e operacao de campo mantidos fora de escopo: PASS
- backlog, `STATUS.md` e `docs/ops/*` atualizados: PASS

## Impacto em negocio e arquitetura
- a oitava superficie de negocio do frontend volta a ficar ativa sem quebrar a fundacao visual da FC-007
- a consolidacao encerra a rodada de reabertura controlada das superficies legadas prioritarias sem abrir novos contratos backend

## PRs ou commits relevantes
- sem commit ou PR nesta execucao

## Proxima task oficial
- nenhuma `READY` oficial no momento
