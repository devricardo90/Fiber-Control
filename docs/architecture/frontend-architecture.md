# Arquitetura Frontend

Frontend ainda nao iniciado.

## Diretriz inicial
- `apps/web` sera implementado apenas apos consolidacao da API e das regras de negocio centrais.
- Quando iniciado, deve consumir contratos tipados compartilhados de `packages/types`.
